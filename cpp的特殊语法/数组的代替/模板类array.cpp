#include<iostream>
#include<cstdio>
#include<array>
using namespace std;
int main()
{
    array<int,5>ai;
    array<double,4>ad={1.2,2.1,3.43,4.3};
    //array<typename,n_elem>arr;
    //与创建vector变量不同的是。n_elem不能是变量
    //在c++11中，可将列表初始化用于vector和array对象，但是在c++98中，不能对vector这么做
}