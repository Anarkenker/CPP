#include<iostream>
#include<vector>
using namespace std;
int main()
{
    vector<int>vi;
    int n;
    cin>>n;
    vector<double>vld;
    //vector<<typename>vt<n_elem>;
    //意思是创建一个名字为vt的vector对象，它可以储存n_elem个类型为typename的元素。
    //e_elem;可以是整型常量也可以是整型变量，是一种更高级的数组，相当于以前的动态数组。
    