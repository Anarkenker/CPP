#include<stdio.h>
#include<string.h>
#include<math.h>
#include<stdlib.h>
int main(int arge,char const*argv[])//
{
    char a[][10];
    char *a[];
//可以用于平替上面的这个而且可以打破二维数组的需要限定列数的限制。
//但是不一样的，因为*a[]中的元素是作为指针指向一个字符串

}